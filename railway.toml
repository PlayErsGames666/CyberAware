[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd myworld/cyberaware && gunicorn cyberaware.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
